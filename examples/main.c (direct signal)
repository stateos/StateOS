#include <stm32f4_discovery.h>
#include <os.h>

OS_TSK(sla, 0, slave);
OS_TSK(mas, 0, master);

void slave()
{
	tsk_wait(1);
	LED_Tick();
}

void master()
{
	tsk_delay(SEC);
	tsk_give(sla, 1);
}

int main()
{
	LED_Init();

	tsk_start(sla);
	tsk_start(mas);
	tsk_stop();
}
